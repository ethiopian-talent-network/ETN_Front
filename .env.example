# API Configuration
# Replace with your actual backend API URL
# Note: Backend has mixed routing - some routes use /api prefix, some don't
# So we use the base URL without /api prefix
VITE_API_BASE_URL=http://localhost:5000
